## Auto generated providers.tf ##
## Updated on: {{ timestamp }} ##

provider "aws" {
  region = "{{ provider_region }}"
  assume_role {
    role_arn    = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by                  = "AFT"
    }
  }
}

# Account where SSO is configured
provider "aws" {
  region = "{{ provider_region }}"
  alias = "sso"
  assume_role {
    role_arn    = "arn:aws:iam::565393065473:role/AWSAFTExecution" # sso account
  }
  default_tags {
    tags = {
      managed_by                  = "AFT"
    }
  }
}